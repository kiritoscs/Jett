{{- if .Values.components.ppocrv4Server.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ppocrv4-server
  namespace: {{ .Values.namespace }}
  labels:
    app: ppocrv4-server
    component: ocr
    model: ppocrv4-server
    app.kubernetes.io/part-of: jett
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ppocrv4-server
  template:
    metadata:
      labels:
        app: ppocrv4-server
    spec:
{{- if .Values.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.imagePullSecrets | indent 8 }}
{{- end }}
      containers:
      - name: ppocrv4-server
        image: "{{ .Values.imageRegistry }}/{{ .Values.images.ppocrv4Server.repository }}:{{ .Values.images.ppocrv4Server.tag }}"
        imagePullPolicy: {{ .Values.images.ppocrv4Server.pullPolicy }}
        ports:
        - containerPort: 8000
        env:
          - name: OMP_NUM_THREADS
            value: "2"
          - name: MKL_NUM_THREADS
            value: "2"
          - name: CPU_NUM
            value: "2"
          - name: MKL_DEBUG_CPU_TYPE
            value: "5"
          - name: KMP_DUPLICATE_LIB_OK
            value: "TRUE"
          - name: PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK
            value: "True"
        resources:
          requests:
            cpu: {{ .Values.resources.ppocrv4Server.requests.cpu | quote }}
            memory: {{ .Values.resources.ppocrv4Server.requests.memory | quote }}
          limits:
            cpu: {{ .Values.resources.ppocrv4Server.limits.cpu | quote }}
            memory: {{ .Values.resources.ppocrv4Server.limits.memory | quote }}
{{- with index .Values.resources.ppocrv4Server.limits "nvidia.com/gpu" }}
            nvidia.com/gpu: {{ . | quote }}
{{- end }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: {{ .Values.modelProbes.ppocrv4Server.livenessInitialDelay }}
          periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: {{ .Values.modelProbes.ppocrv4Server.readinessInitialDelay }}
          periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
---
{{- end }}
